
$("#favbtn<%=@pid%>").off().on("click", function(){

  if($(this).hasClass('btn btn-success')){
    $(this).removeClass('btn btn-success').addClass('btn btn-info');
      }
      else
      {
         $(this).removeClass('btn btn-info').addClass('btn btn-success');
      }
});
